# SmartJobMatcher 🎯

Умный матчер вакансий и резюме на основе LLM (Large Language Models). Использует локальный Ollama для анализа текста и детерминированный алгоритм для точного скоринга соответствия.

## Возможности

- ✅ Автоматический парсинг вакансий и резюме с помощью LLM
- ✅ Детерминированный алгоритм оценки совместимости (0-100 баллов)
- ✅ Детальный отчёт по всем критериям (образование, опыт, hard/soft skills)
- ✅ Генерация человекопонятного фидбэка
- ✅ Полное логирование процесса анализа
- ✅ Сохранение результатов в JSON
- ✅ Гибкая конфигурация через config.json
- ✅ Модульная архитектура (пакет `matcher`)
- ✅ Обратная совместимость со старым API

## Требования

- Python 3.8+
- [Ollama](https://ollama.com/) - локальный LLM сервер
- Модель llama3.2:3b (или другая на выбор)

## Установка

### 1. Установите Ollama

```bash
# Linux
curl -fsSL https://ollama.com/install.sh | sh

# macOS
brew install ollama

# Windows
# Скачайте с https://ollama.com/download
```

### 2. Загрузите модель

```bash
ollama pull llama3.2:3b
```

### 3. Запустите Ollama сервер

```bash
ollama serve
```

### 4. Установите зависимости Python

```bash
pip install -r requirements.txt
```

## Структура проекта

```
analyst_exe/
├── matcher/                  # Основной пакет ⭐ НОВОЕ
│   ├── __init__.py          # Экспорт SmartJobMatcher, Config
│   ├── core.py              # SmartJobMatcher класс
│   ├── config.py            # Config класс
│   └── README.md            # Подробная документация API
├── smart_job_matcher.py     # Обратная совместимость (deprecated)
├── example.py               # Примеры использования
├── config.json              # Конфигурация
├── requirements.txt         # Зависимости
├── README.md                # Основная документация
├── QUICKSTART.md            # Быстрый старт за 5 минут
└── examples/
    ├── sample_data.json     # Тестовые данные
    └── test_with_samples.py # Интерактивное тестирование
```

## API

### Новый способ (рекомендуется)

```python
from matcher import SmartJobMatcher, Config

# С конфигурационным файлом
config = Config("config.json")
matcher = SmartJobMatcher(config=config)

# Или без конфига (настройки по умолчанию)
matcher = SmartJobMatcher()

result = matcher.match(job_description, resume_text)
```

### Старый способ (обратная совместимость)

```python
from smart_job_matcher import SmartJobMatcher

matcher = SmartJobMatcher()
result = matcher.match(job_description, resume_text)
```

📖 **Детальная документация API:** см. [matcher/README.md](matcher/README.md)

## Быстрый старт

### Вариант 1: Запуск демо

```bash
python smart_job_matcher.py
```

Это запустит встроенный пример с тестовыми данными.

### Вариант 2: Использование в коде

```python
from smart_job_matcher import SmartJobMatcher

# Инициализация
matcher = SmartJobMatcher()

# Ваши данные
job_description = """
Требуется Python разработчик.
Опыт: от 3 лет.
Навыки: FastAPI, PostgreSQL, Docker, Git.
Образование: высшее техническое.
"""

resume = """
Опыт: 4 года Python разработки.
Навыки: Django, FastAPI, PostgreSQL, Docker, CI/CD.
Образование: МФТИ, прикладная математика.
"""

# Анализ
result = matcher.match(job_description, resume)

# Результаты
print(f"Скор: {result['score']}/100")
print(f"Фидбэк: {result['feedback']}")

# Сохранение
matcher.save_result(result, "my_analysis.json")
```

## Структура проекта

```
analyst_exe/
├── smart_job_matcher.py  # Основной класс
├── config.json           # Конфигурация
├── requirements.txt      # Зависимости
├── README.md            # Документация
├── example.py           # Пример использования
└── results/             # Папка для результатов (создаётся автоматически)
```

## Конфигурация

Настройки в `config.json`:

```json
{
  "ollama": {
    "model": "llama3.2:3b",      // Модель LLM
    "url": "http://localhost:11434/api/generate",
    "timeout": 60,                // Таймаут в секундах
    "temperature": 0.1            // Креативность (0-1)
  },
  "scoring": {
    "weights": {
      "education_match": 25,      // Вес образования
      "experience_match": 25,     // Вес опыта
      "hard_skills_match": 40,    // Вес технических навыков
      "soft_skills_match": 10     // Вес личностных качеств
    }
  }
}
```

### Настройка весов

Общая сумма весов должна быть **100**. Распределяйте в зависимости от приоритетов:

- **Для технических вакансий**: увеличьте `hard_skills_match` до 50-60
- **Для менеджмента**: увеличьте `soft_skills_match` и `experience_match`
- **Для стажировок**: уменьшите `experience_match`, увеличьте `education_match`

## Формат результата

```json
{
  "score": 75,
  "report": {
    "score_details": {
      "education": 25,
      "experience": 20.83,
      "hard_skills": 26.67,
      "soft_skills": 6.67
    },
    "strengths": [
      "Релевантное образование",
      "Опыт: 2.5 лет (требуется 3)",
      "✓ word",
      "✓ excel"
    ],
    "missing_required": [
      "✗ хроматографы",
      "✗ спектрофотометры"
    ],
    "partial_match": [
      "Опыт: 2.5 лет (требуется 3)"
    ]
  },
  "feedback": "Хорошее соответствие. Кандидат обладает релевантным образованием...",
  "debug": {
    "parsed_job": { ... },
    "parsed_resume": { ... },
    "timestamp": "2025-01-07T15:30:00"
  }
}
```

## Алгоритм оценки

1. **Парсинг** - LLM извлекает структурированные данные из текста
2. **Скоринг** - Детерминированный расчёт по формуле:
   - Образование: полное совпадение = 25 баллов
   - Опыт: пропорционально требуемому (мин 0, макс 25)
   - Hard Skills: распределение 40 баллов по навыкам
   - Soft Skills: распределение 10 баллов по качествам
3. **Фидбэк** - LLM генерирует читаемый отчёт

## Логирование

Все действия логируются в `job_matcher.log`:

```
2025-01-07 15:30:00 - INFO - ✓ Ollama сервер доступен
2025-01-07 15:30:05 - INFO - Успешно распарсен резюме кандидата
2025-01-07 15:30:10 - INFO - Рассчитан итоговый скор: 75/100
```

## Устранение проблем

### Ollama недоступен

```
✗ Ollama сервер недоступен
```

**Решение:**
```bash
# Запустите Ollama
ollama serve

# В другом терминале проверьте
curl http://localhost:11434/api/tags
```

### Модель не найдена

```
Error: model 'llama3.2:3b' not found
```

**Решение:**
```bash
ollama pull llama3.2:3b
```

### Медленная работа

1. Используйте более лёгкую модель: `llama3.2:1b`
2. Увеличьте `timeout` в config.json
3. Убедитесь, что Ollama использует GPU (если доступен)

## Альтернативные модели

```bash
# Легче и быстрее
ollama pull llama3.2:1b

# Точнее, но медленнее
ollama pull llama3:8b
ollama pull llama3:70b  # Требует много RAM

# Специализированные
ollama pull mistral:7b
ollama pull phi3:3.8b
```

После установки обновите `config.json`:

```json
{
  "ollama": {
    "model": "llama3.2:1b"
  }
}
```

## Расширение функционала

### Добавление API сервера

```python
from fastapi import FastAPI
from smart_job_matcher import SmartJobMatcher

app = FastAPI()
matcher = SmartJobMatcher()

@app.post("/match")
def match_endpoint(job: str, resume: str):
    return matcher.match(job, resume)
```

### Пакетная обработка

```python
matcher = SmartJobMatcher()

jobs = [...]  # Список вакансий
resumes = [...]  # Список резюме

for job in jobs:
    for resume in resumes:
        result = matcher.match(job, resume)
        if result['score'] >= 70:
            matcher.save_result(result)
```

## Лицензия

MIT License

## Поддержка

Для вопросов и предложений создавайте Issues в репозитории.

## Roadmap

- [ ] REST API сервер
- [ ] Веб-интерфейс
- [ ] Поддержка PDF/DOCX
- [ ] Интеграция с HH.ru API
- [ ] Batch processing
- [ ] Кэширование результатов
- [ ] Мультиязычность
