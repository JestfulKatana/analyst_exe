# Быстрый старт 🚀

Пошаговая инструкция для немедленного запуска SmartJobMatcher.

## Шаг 1: Установка Ollama (5 минут)

### Linux
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

### macOS
```bash
brew install ollama
```

### Windows
Скачайте установщик с https://ollama.com/download

## Шаг 2: Загрузка модели (2-3 минуты)

```bash
ollama pull llama3.2:3b
```

Это загрузит ~2GB модели. Подождите завершения.

## Шаг 3: Запуск Ollama сервера

```bash
ollama serve
```

Оставьте этот терминал открытым!

## Шаг 4: Установка зависимостей Python

В новом терминале:

```bash
pip install -r requirements.txt
```

## Шаг 5: Запуск демо

```bash
python smart_job_matcher.py
```

Или с примерами:

```bash
python example.py
```

Или интерактивное тестирование:

```bash
python examples/test_with_samples.py
```

## Готово! 🎉

Теперь вы можете использовать SmartJobMatcher для анализа вакансий и резюме.

## Что дальше?

1. Прочитайте [README.md](README.md) для детальной документации
2. Настройте веса в [config.json](config.json) под свои нужды
3. Изучите [example.py](example.py) для разных сценариев использования
4. Попробуйте [примеры данных](examples/sample_data.json)

## Проблемы?

### Ollama не запускается
```bash
# Проверьте статус
curl http://localhost:11434/api/tags

# Если не работает, перезапустите
killall ollama
ollama serve
```

### Python ошибки
```bash
# Проверьте версию Python (нужна 3.8+)
python --version

# Переустановите зависимости
pip install --upgrade -r requirements.txt
```

### Медленно работает
- Используйте более лёгкую модель: `ollama pull llama3.2:1b`
- Измените модель в коде: `SmartJobMatcher(ollama_model="llama3.2:1b")`
